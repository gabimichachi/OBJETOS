function somar() {
    let v1 = 0;
    let v2 = 0;
    const resultado = v1 + v2;
    console.log(resultado);
}

const soma = somar()

// parametros para o calculo
function somar(v1, v2){
    const resultado = v1 +
}

// 01. crie uma função que retorne a porcentagem de um valor
// 02. crie uma função que retorne o valor de uma compra a prazo 
// 03. crie uma função que retorne o valor de uma compra a vista 

// resolução 01 função de seta (arrow function)
let calcPorcentagem = (valor, taxa) =>{
    return valor * (taxa/100);
}




// retornando o valor
function somar(v1, v2){
    const resultado = v1 + v2;
    return resultado
}

// retornando apenas o valor informado no parametro
function somar(v1, v2){
    return(v1 + v2)
}


// 01 - crie uma funcao que retorne a porcentagem de um valor
function calcPorcentagem(valor,taxa){
    return valor * (taxa / 100);
}




// 02 - crie uma funcao



// resolucao com funcao de seta
let calcPorcantagem = (valor, taxa) =>{
    return valor * (taxa / 100);
}